apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayParameters
metadata:
  name: tracing-config
  namespace: default
spec:
  logging:
    level: debug
  deployment:
    spec:
      template:
        spec:
          volumes:
          - name: config-volume
            configMap:
              name: agentgateway-config
