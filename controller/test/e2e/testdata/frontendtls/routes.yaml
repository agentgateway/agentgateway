apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: http-route
  namespace: agentgateway-frontendtls
spec:
  hostnames:
  - "*.example.com"
  parentRefs:
    - name: gateway
  rules:
  - backendRefs:
    - name: backend
      port: 80
